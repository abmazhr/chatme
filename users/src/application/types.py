from typing import (
    Any,
    Dict,
    Tuple,
    List,
    Optional as Maybe,
    Union as Either,
    NamedTuple,
    Callable,
    TypeVar,
)
from enum import Enum
from dataclasses import dataclass, field

SimpleConfig = Dict[str, Any]
