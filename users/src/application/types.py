from typing import (
    Any,
    Dict,
    Tuple,
    List,
    Optional as Maybe,
    Union as Either,
    NamedTuple,
    Callable,
    TypeVar,
)
from enum import Enum

SimpleConfig = Dict[str, Any]
